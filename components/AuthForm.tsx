"use client";import {FormEvent,useState}from"react";import Link from"next/link";import{useRouter}from"next/navigation";import{AppShell,Brand,Button,useI18n}from"./AppShell";
export function AuthForm({mode}:{mode:"login"|"signup"}){return <AppShell><AuthInner mode={mode}/></AppShell>}
function AuthInner({mode}:{mode:"login"|"signup"}){const{t,lang}=useI18n(),r=useRouter(),[error,setError]=useState(""),[busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");const fd=new FormData(e.currentTarget),password=String(fd.get("password"));if(mode==="signup"&&password!==fd.get("confirm")){setError("Passwords do not match");setBusy(false);return}const res=await fetch(`/api/auth/${mode}`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({username:fd.get("username"),password,uiLanguage:lang})});const json=await res.json();if(!res.ok){setError(json.error);setBusy(false);return}r.push("/menu");r.refresh()}return <main className="center"><Brand/><form className="card auth" onSubmit={submit}><label>{t.username}<input name="username" required minLength={3} autoComplete="username" placeholder="ex:yacine"/></label><label>{t.password}<input name="password" type="password" required minLength={8}/></label>{mode==="signup"&&<label>{t.confirm}<input name="confirm" type="password" required minLength={8}/></label>}{error&&<p className="error" role="alert">{error}</p>}<Button disabled={busy}>{busy?t.loading:mode==="login"?t.login:t.create}</Button><p className="switch">{mode==="login"?t.newHere:t.have} <Link href={mode==="login"?"/signup":"/login"}>{mode==="login"?t.signup:t.login}</Link></p></form></main>}
